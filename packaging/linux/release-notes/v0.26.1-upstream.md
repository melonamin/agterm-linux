### Improved

- The bundled agent skill description is short enough to fit the agent-listing limit, so its complete trigger
  guidance now reaches the model. #528 @umputun

### Bug Fixes

- Live-session commands longer than 1024 bytes now go to zmx as a create-only payload instead of passing
  through the pty, preventing truncation and also delivering commands to newly created split panes.
  #533 @umputun
