# Upstream v0.16.1 Linux parity

## Scope

- Upstream range: `v0.15.2..v0.16.1`.
- Upstream target: `d597d059edb99c69675e74b9f2ba159bdc194cc6`.
- Linux baseline: `linux-v0.15.2+linux.2` at `97bcb8810a011c5cd5d8494a05d00e7aed35e05a`.
- Merge commit: `607ffe9` (`chore(upstream): merge v0.16.1`).
- Exclude unrelated generated artifacts and `site/_screenshot.png`.
- Keep `agtermCore/Tests/agtermCoreTests/ConfigPathsTests.swift` identical to upstream.
- Do not add downstream release notes or change the Linux release boundary until parity validation passes.

## Integration rules

- Preserve upstream wire shapes, validation messages, persistence semantics, and CLI behavior.
- Keep GTK, Glibc socket handling, libghostty configuration, and Linux process behavior under `agterm-linux/`.
- Reuse `WindowLibrary`'s upstream `ControlEventRing`; do not create a Linux-only event model.
- Retain the portable shared `AgtermctlCommandCatalog` extension point and add upstream `events` to that catalog.
- Keep mutable state observable through the upstream tree/event read-back fields.
- Use native GTK workspace expansion and Linux terminal-surface behavior without adding divergent commands.

## Parity inventory

### Shared host-free behavior inherited directly

- [x] Merge command-session `--wait` protocol, persistence, CLI parsing, and tests.
- [x] Merge per-pane restore overrides, persistence, control validation, tree read-back, and CLI parsing.
- [x] Merge per-workspace collapse/expand commands and persisted `Workspace.isExpanded` state.
- [x] Merge bounded control-event history, cursor protocol, `events.read`, streaming CLI, and bundled skill documentation.
- [x] Merge Codex final-question classification and its shared tests.
- [x] Merge the shared `cursor-click-to-move = false` Ghostty default.

### Linux adapters and native UI

- [x] Route `events.read` through the Linux socket and `WindowLibrary.readEvents`.
- [x] Confirm Linux status, notification, session lifecycle, and tree mutations feed the shared event ring.
- [x] Pass `session new --wait` into Linux session creation and the primary surface's wait-after-command behavior.
- [x] Implement `session.restore` in the Linux `ControlActions` adapter and use restore overrides when creating primary and split surfaces.
- [x] Route `workspace.collapse` and `workspace.expand` to persisted model state and synchronize the GTK sidebar.
- [x] Restore persisted GTK workspace expansion at launch and keep row-toggle behavior in sync.
- [x] Apply session watermarks to scratch surfaces as well as primary and split surfaces.
- [x] Adopt the upstream Codex final-question behavior without regressing Linux pane-ID forwarding.
- [x] Verify the Linux config chain actually loads the cursor-click default through `GhosttyDefaults.baseConfLines`.

### Control and documentation surfaces

- [x] Include upstream `events`, `session restore`, and workspace expansion commands in the shared command catalog.
- [x] Add Linux-focused unit and socket round-trip coverage for every adapter path.
- [x] Update Linux feature-parity text after model, build, and adapter validation.
- [x] Confirm the bundled agent skill remains accurate for all 65 shared commands.

### Platform exemptions

- [x] AppKit `flagsChanged` codepoint handling is macOS-only; GTK does not use `NSEvent` or that translation path.
- [x] Upstream Homebrew, DMG, notarization, and Finder wording remains excluded from Linux release behavior.
- [x] No separate GTK GUI is required for `events` or per-pane restore overrides because upstream defines both as control-native capabilities.

## Validation

- [x] `cd agtermCore && mise x swift@6.3.2 -- swift test`
- [x] `mise x swiftlint@0.65.0 -- swiftlint lint --strict --quiet`
- [x] `scripts/check-linux-cli-drift.sh`
- [x] `scripts/check-linux-core-boundary.sh v0.16.1`
- [x] `scripts/test-linux-resources.sh`
- [x] `scripts/setup-linux.sh`
- [x] `cd agterm-linux && mise x swift@6.3.2 -- swift test`
- [x] `cd agterm-linux && mise x swift@6.3.2 -- swift build --product agtermctl-linux`
- [x] `cd agterm-linux && mise x swift@6.3.2 -- swift build --product AgtermLinux`
- [x] `cd agterm-linux && mise x swift@6.3.2 -- swift build -c release`
- [x] `scripts/test-linux-ui.sh` (GitHub Actions run `29968403889`)
- [x] Focused live control round trips for events, restore overrides, workspace expansion, and `--wait`.
- [x] `git diff --check`
- [x] Full downstream-boundary and protected-test audit against `v0.16.1`.
- [x] Green GitHub branch CI for the exact parity implementation commit (`259a4f1`, run `29968403889`).

Local UI/control validation is deferred to branch CI because this host does not have `xvfb-run`, Openbox,
or xdotool.
GTK Broadway is installed, but this libghostty/GTK build cannot open a Broadway display.

## Release follow-up

- [x] Move this plan to `docs/plans/completed/` only after all parity and validation items pass.
- [x] Update CI and release workflow upstream-boundary pins to `v0.16.1` and its exact commit.
- [x] Prepare `packaging/linux/release-notes/v0.16.1.md` from verified upstream and Linux behavior.
- [x] Generate and review the complete Linux release body before requesting tag approval.
