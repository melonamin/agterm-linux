# Plan: Linux v0.14.0 parity, review fixes, and verified release

Bring `linux-port` to the exact upstream `v0.14.0` release, close every defect found in the deep Linux-port review,
and publish a verified `linux-v0.14.0` release from `linux-port`.
The implementation must preserve agterm as one product: shared behavior stays portable and upstream-compatible,
while GTK/libadwaita, Linux process handling, libghostty FFI, desktop integration, packaging, and release mechanics
stay in Linux-owned targets.

The selected upstream boundary is the immutable `v0.14.0` tag at `ed0e4c6`.
Do not roll unreleased upstream work into this release.
At plan-writing time `upstream/master` has two known post-tag commits, `8496d9e` and `06a5dff`; fetch again during
execution and exclude those plus any later post-tag commits from both `master` and `linux-v0.14.0`.

The terminal state is a public GitHub release named `linux-v0.14.0` whose tag points to the validated candidate on
`origin/linux-port`, with tar, DEB, RPM, AppImage, checksums, and attestations all present and independently checked.
The release payload must contain the pinned Ghostty theme catalog and a working `xterm-ghostty` terminfo entry.

The following constraints apply to every task:

- `agtermCore/Sources/agtermCore/` must not import or call GTK, libadwaita, AppKit, SwiftUI, Metal, GhosttyKit,
  libghostty FFI, Glibc, CoreGraphics, or another host runtime.
- Any shared-package delta must use portable data and be suitable for upstream agterm.
  Prepare such work independently from Linux-only changes.
- Never make a downstream edit to
  `agtermCore/Tests/agtermCoreTests/ConfigPathsTests.swift`.
- Accept upstream's `v0.14.0` `CHANGELOG.md` verbatim during the merge, but do not add downstream release notes to it.
- Keep Linux-specific behavior in `agterm-linux/`, `linux/`, `packaging/linux/`, Linux workflows, or Linux scripts.
- Preserve existing control commands and read-back.
  Dashboard chrome and notification-click routing are explicitly control-API exempt because they compose existing
  `dashboard`, `window.select`, and `session.select` behavior rather than introduce a new state mutation.
- Preserve the existing Linux Dock-bounce exemption: Linux desktop environments expose no portable equivalent of
  macOS Dock attention requests.
- Keep commits narrow: upstream synchronization, portable seams, each safety fix, parity features, CI, and release
  preparation must remain reviewable independently.
- Stop for explicit approval immediately before pushing `master` or `linux-port`, publishing an upstream PR branch,
  creating or pushing `linux-v0.14.0`, moving a remote tag, or publishing the final release text.

## Validation Commands

Run these from the absolute repository root with the versions pinned by Linux CI and the release workflow.
Commands that inspect a public release run only after the tag workflow publishes it.

- `git status --short --branch`
- `git merge-base --is-ancestor v0.14.0 linux-port`
- `git log --oneline v0.14.0..master`
- `git diff --exit-code v0.14.0 -- agtermCore/Tests/agtermCoreTests/ConfigPathsTests.swift`
- `scripts/check-linux-core-boundary.sh v0.14.0`
- `(cd agtermCore && mise x swift@6.3.2 -- swift test)`
- `(cd agterm-linux && mise x swift@6.3.2 -- swift test)`
- `mise x swiftlint@0.65.0 -- swiftlint lint --strict --quiet`
- `scripts/setup-linux.sh`
- `scripts/verify-linux-resources.sh agterm-linux/vendor/ghostty/share`
- `(cd agterm-linux && mise x swift@6.3.2 -- swift build --product agtermctl-linux)`
- `(cd agterm-linux && mise x swift@6.3.2 -- swift build --product AgtermLinux)`
- `(cd agterm-linux && mise x swift@6.3.2 -- swift build -c release)`
- `scripts/test-linux-ui.sh`
- `git diff --check v0.14.0...HEAD`
- `make packages-linux VERSION=0.14.0`
- `scripts/verify-linux-packages.sh 0.14.0 dist-linux`
- `gh run list --repo melonamin/agterm-linux --branch linux-port --limit 10`
- `gh release view linux-v0.14.0 --repo melonamin/agterm-linux --json url,tagName,targetCommitish,assets`
- `scripts/verify-published-linux-release.sh linux-v0.14.0`

### Task 1: Synchronize to the exact upstream v0.14.0 boundary

Establish a clean, tag-bounded baseline before changing behavior.
`master` must become an exact upstream-tracking ref for `v0.14.0`, and `linux-port` must contain that tag through a
normal merge without absorbing post-tag upstream commits.

- [x] Re-read `README.md`, `ARCHITECTURE.md`, root `AGENTS.md`, and every matching `.claude/rules/` note before
  touching control, release, notification, window, libghostty, settings, or UI-test code.
- [x] Inspect the worktree, all worktrees, branches, remotes, tags, recent commits, and existing fork releases.
  Preserve user changes and leave unrelated files, especially `site/_screenshot.png`, untouched.
- [x] Fetch `upstream` and `origin`, including tags, and query both repositories explicitly with `gh --repo`.
  Record the current upstream stable tag, commits after it, the newest upstream tag contained in `linux-port`, and
  the newest `linux-v*` fork release.
- [x] Verify that the selected source is exactly upstream tag `v0.14.0` at `ed0e4c6`.
  Record and exclude every commit in `v0.14.0..upstream/master`; do not silently change to rolling parity.
- [x] Fast-forward local `master` to `v0.14.0` and verify `git log v0.14.0..master` is empty.
  Do not push it yet.
- [x] Fast-forward local `linux-port` from `origin/linux-port`, then merge local `master` with a normal merge commit.
  Resolve shared files toward upstream unless a carried fix is proven portable, necessary, and upstreamable.
- [x] Accept upstream's `CHANGELOG.md`, Pi resource, dashboard controller changes, docs, and protected core tests
  unchanged during the merge.
- [x] Verify `v0.14.0` is an ancestor of `linux-port`, the protected `ConfigPathsTests.swift` has no downstream diff,
  and the worktree contains no accidental post-tag upstream content.
- [x] Save a machine-readable inventory of `v0.13.0..v0.14.0` changes and their Linux disposition for the final parity
  audit in Task 12.

### Task 2: Restore clear core and Linux ownership boundaries

Replace the `LinuxCoreCompatibility.swift` grab bag with explicit ownership.
Portable model decisions should use existing shared APIs or small upstream-compatible seams; Linux shortcuts,
resource lookup, GTK presentation, process behavior, and filesystem adapters must remain in the Linux targets.

- [x] Inventory every declaration in `agterm-linux/Sources/AgtermLinux/LinuxCoreCompatibility.swift` and classify it
  as shared portable behavior, Linux policy, or Linux host adapter before moving anything.
- [x] Replace duplicated `AppStore` mutations and notification policy with existing public `agtermCore` behavior where
  available.
  Add a shared seam only when both hosts can use the same primitive-data contract and cover it with host-free tests.
- [x] Keep Linux key chords, starter configuration text, Ghostty resource resolution, POSIX `/proc` decoding,
  desktop delete wording, theme-file IO, and GTK-facing helpers in narrowly named Linux files.
  Do not move them into `agtermCore` merely because their current implementation is pure Swift.
- [x] Remove `LinuxCoreCompatibility.swift` after every declaration has a clear owner and no compatibility-only
  duplicate remains.
- [x] Add `scripts/check-linux-core-boundary.sh` to reject prohibited host imports or symbols in
  `agtermCore/Sources/agtermCore`, reject a downstream protected-test diff, and print the allowed portable package
  delta from `v0.14.0` for review.
- [x] Add or relocate unit tests beside the owning module, including model mutation and pure Linux policy coverage.
- [x] Keep all touched source files within configured lint limits; split a newly overlong file by responsibility rather
  than raising limits.

### Task 3: Make Ghostty themes and terminfo deterministic release inputs

Fix the published-package defect where every `linux-v0.13.0` artifact shipped without Ghostty themes and without a
terminfo database while the runtime still selected `TERM=xterm-ghostty`.
All four package formats share one staged payload, so validate that payload strictly before any archive is produced.

- [x] Reproduce the empty-resource failure from a clean vendor directory with no system Ghostty theme installation,
  and turn the reproduction into an automated resource-verification test.
- [x] Change `scripts/setup-linux.sh` to obtain the complete theme catalog from the pinned Ghostty source/dependency,
  never from `/usr/share`, `/usr/local/share`, or a maintainer's user directory.
  Record the resource provenance and expected catalog count or manifest for the Ghostty revision.
- [x] Compile `src/terminfo/ghostty.terminfo` with `tic -x` from the same pinned Ghostty checkout.
  Add the required `ncurses-bin`/`tic` dependency to Linux CI and release setup and fail setup when compilation or
  `infocmp` validation fails.
- [x] Make the vendored-resource cache validity check include `libghostty.so`, headers, shell integration, the theme
  manifest, and `share/terminfo/x/xterm-ghostty`.
  A cache containing only the library must be treated as stale and repaired.
- [x] Add `scripts/verify-linux-resources.sh` to require shell integration, a non-empty expected theme catalog, known
  pinned theme files, and a readable `xterm-ghostty` entry via `infocmp -A`.
- [x] Make `scripts/stage-linux.sh` fail release staging if required vendored resources are absent.
  Development may still use an explicitly reported system fallback, but release packaging may not.
- [x] Change `GhosttyResourceResolver` and default-config generation so `TERM=xterm-ghostty` is selected only when the
  resolved resource root has both shell integration and the sibling compiled terminfo entry.
  Otherwise unset the resource override as appropriate and use `TERM=xterm-256color`.
- [x] Add Linux unit tests for complete resources, shell-integration-only resources, terminfo-only resources, malformed
  candidates, precedence, and the `TERM` decision.
- [x] Extend `scripts/verify-linux-packages.sh` to inspect tar, DEB, RPM, and AppImage payloads for the expected theme
  catalog, shell integration, and terminfo entry, and to run `infocmp` against each extracted database.
- [x] Fix the trailing whitespace in `linux/patches/ghostty-embedded-opengl.patch` and require
  `git diff --check v0.14.0...HEAD` in CI.

### Task 4: Correct libghostty C-string and callback lifetimes

Eliminate the length-delimited URL over-read and the surface-configuration use-after-lifetime risk.
Every C buffer crossing a libghostty boundary must have an explicit owner and release point, and callback data must be
copied before actor or queue hops.

- [x] Decode `GHOSTTY_ACTION_OPEN_URL` from exactly `open_url.len` bytes into Swift-owned storage before launching the
  URI.
  Do not call `String(cString:)` on the non-NUL-terminated buffer.
- [x] Add focused tests with a non-NUL-terminated byte buffer, trailing sentinel bytes, empty input, and invalid UTF-8
  to prove the decoder never reads past `len` and fails safely.
- [x] Replace short-lived `withCString` configuration for `cwd`, `command`, and `initialInput` with `strdup`-backed
  buffers owned by `GhosttySurface` for its full lifetime.
- [x] Store the `ghostty_env_var_s` array as surface-owned storage as well as retaining each key/value buffer, so the
  array and its members remain valid for every libghostty consumption path.
- [x] Free all retained configuration strings and clear the environment array only after `ghostty_surface_free`.
  Make teardown and deinitialization idempotent so partial surface creation, process exit, pane close, and window close
  cannot double-free or leak them.
- [x] Audit every other libghostty callback for borrowed C data that crosses `runOnMain`, a GLib source, an actor, or a
  stored closure, and copy such data synchronously before the boundary.
- [x] Add Linux tests for partial surface creation and repeated teardown, plus realistic runtime checks for restored
  initial input, session `--command`, overlay commands, and non-default working directories.
- [x] Run a real link-hover/open action in an isolated app and confirm the exact URI reaches the platform launcher
  without truncation, garbage suffixes, or a crash.

### Task 5: Bind GTK callbacks and timers to their owning window

Remove per-window routing through the mutable frontmost `gController` pointer.
Widget callbacks, delayed work, and asynchronous completions must retain or identify the `AppController` that created
them; `gController` may remain only as the deliberate resolver for truly app-global frontmost actions.

- [x] Inventory every `gController` read in GTK signal callbacks, GLib timers, idle callbacks, process completions,
  theme/palette/search/settings handlers, sidebar rows, context menus, and terminal callbacks.
- [x] Pass the owning controller through GTK `user_data`, a widget-owned context, or a stable window identifier for
  every per-window signal.
  Define one safe retain/release convention and use GTK destroy notifiers where contexts outlive a lexical scope.
- [x] Keep only explicitly global application actions on the frontmost-controller resolver, and document that small
  allowlist next to `gController`.
- [x] Replace the raw `GtkPaned` pointer timer with an owner-scoped split-ratio restore coordinator carrying window id,
  session id, paned identity, and generation.
  Track its GLib source id, cancel it on superseding layout, session removal, and window close, and clear
  `splitCaptureSuppressed` on every success, cancellation, missing-widget, and missing-session path.
- [x] Route theme picker, command palette, search bar, preferences, row context menus, rename callbacks, and leader
  timeout callbacks back to the originating controller even when another window becomes active.
- [x] Route late window-state notifications through their supplied controller context so closing a secondary window
  cannot dereference a retired or newly frontmost controller.
- [x] Add unit coverage for split-ratio restore cancellation and a two-window regression test that activates another
  window while a restore timer is pending, then verifies the original session remains resizable and persistable.
- [x] Extend the AT-SPI smoke suite with background-window palette/settings/context-menu activity and repeated
  secondary-window close while callbacks are pending; assert the process stays alive and each mutation lands in the
  correct window.

### Task 6: Fix notification suppression, pane identity, and closed-window reveal

Make Linux notification behavior match the shared policy and macOS navigation semantics.
Suppression must inspect the actual firing GTK surface, and a notification click must honor the encoded window id even
after that window has been closed.

- [x] Give every `GhosttySurface` an explicit Linux surface role/target instead of deriving notification identity from
  the `isSplitPane` boolean.
  Map main, split, overlay, scratch, and quick surfaces deliberately onto the shared notification identity model.
- [x] Compute `firingIsFocused` from the exact firing widget, its live GTK focus state, and its active window.
  Do not infer focus from `selectedSessionID`, `splitFocused`, or `gController`.
- [x] Apply `TerminalNotification.shouldDeliver` before incrementing `unseenCount` for OSC notifications, so a focused
  active pane gets neither a banner nor a badge.
  Preserve the documented control `notify` behavior: an explicit caller request bypasses focus suppression and still
  updates the badge.
- [x] Use the full window/session/pane identity for system-notification coalescing.
  Update withdrawal so selecting a session removes every delivered pane identity for that session.
- [x] Change the `app.reveal` action to pass the parsed `windowID` into navigation.
  Reopen a closed window with `openWindow(windowID)`, wait until its controller/store is registered if needed, select
  the session, focus the encoded pane, and remain stale-safe for deleted windows, sessions, or splits.
- [x] Add pure routing tests for focused hidden panes, inactive windows, main/split/overlay identities, control-send
  bypass, unseen-count ordering, coalescing, and stale targets.
- [x] Add an isolated integration test that closes the source window, activates its encoded GApplication reveal action,
  verifies `window.list` reports the encoded window reopened, then queries `tree --window <id> --json` and asserts the
  target session is active and its `splitFocused`/surface state matches the encoded pane.
- [x] Verify by hand in the isolated dev instance that a focused pane suppresses both banner and badge, a hidden pane
  does not, and clicking a banner reopens the correct window without touching the user's real notification state.

### Task 7: Make custom-command execution observable and window-safe

Stop swallowing custom-command launch failures and stop routing asynchronous failures to whichever window happens to
be frontmost later.
GUI-launched commands also must not inherit agterm's terminal or diagnostic streams.

- [x] Capture the originating controller incarnation as well as its window id, session, pane, selection, cwd, and
  command name before starting the process.
  Use weak controller identity or a per-controller generation token so closing and reopening the same persisted window
  id cannot make an old completion look current.
  Build `CommandContext` from that captured origin rather than a later frontmost lookup.
- [x] Introduce an injectable Linux process-launch adapter around `Foundation.Process` so launch success, spawn errors,
  exit status, environment, cwd, and stdio policy are unit-testable without GTK.
- [x] Set stdin, stdout, and stderr to `/dev/null` for GUI custom commands unless a future feature deliberately exposes
  captured output.
- [x] Replace `try? proc.run()` with `do/catch` and show a launch-failure toast in the originating window, including the
  command name and concise OS error.
- [x] Route non-zero termination status only to the still-live originating controller incarnation, not `gController`
  and not a fresh controller found by window id alone.
  If that controller has closed, discard the toast safely rather than showing it in another or newly reopened window.
- [x] Preserve the existing `AGTERM_*`, `$AGT_PANE`, selection, working-directory, and shell-expansion semantics.
- [x] Add tests for a missing executable, invalid cwd, non-zero exit, successful exit, environment expansion, all pane
  origins, null stdio, window closure before asynchronous completion, and close-then-reopen of the same persisted
  window id before completion.
- [x] Exercise an invalid and a non-zero custom command in each of two isolated windows and verify the toast appears
  only in the originating window.

### Task 8: Replace the copied Linux CLI with the shared agtermctl implementation

Remove the parser and formatter fork that lost `theme set --light/--dark`, synchronized theme-list output, and
`affected` result formatting.
Use one host-free CLI implementation while retaining the Linux-only `integration` subcommand as a thin extension.

- [x] Create a dedicated portable branch from `v0.14.0` for the minimal `agtermctlKit` reuse seam, with no Linux-only
  files or behavior.
- [x] Export `agtermctlKit` as a Swift package library product and expose a public shared subcommand catalog or root
  configuration hook that lets another host append subcommands without duplicating parser types.
- [x] Make the shared default socket directory use Foundation's platform application-support location while preserving
  `--socket` and `AGTERM_STATE_DIR` precedence.
  Prove the change is behavior-identical on macOS and resolves the Linux XDG path correctly.
- [x] Keep the portable seam host-free and cover it in `agtermctlKitTests`.
  Preserve `Agtermctl` as upstream macOS's root command and add no GTK, Glibc, or Linux integration dependency.
- [x] Keep the portable change as an isolated commit suitable for an upstream PR.
  Do not publish that branch without presenting its exact diff and receiving the separate push/PR approval gate;
  Linux release work does not wait for upstream acceptance.
- [x] Merge or cherry-pick that exact portable commit into `linux-port` before making the Linux package depend on the
  new product.
  Verify the integrated commit is unchanged from the dedicated branch and remains separate from the following
  Linux-only wrapper commit.
- [x] Change `agterm-linux/Package.swift` so `agtermctl-linux` consumes the shared `agtermctlKit` product and defines only
  a Linux root that appends `Integration.self` plus the Linux integration implementation.
- [x] Delete the copied common command and socket-client files under `agterm-linux/Sources/agtermctl/` once the Linux
  root builds entirely from the shared catalog.
- [x] Run the complete shared `agtermctlKitTests` suite and Linux integration CLI tests against the Linux executable.
  Add subprocess parse/output tests proving `theme set --light`, `theme set --dark`, dual-theme list rendering,
  `affected` output, shared help, socket precedence, and the appended `integration` command.
- [x] Add a drift guard that fails if common CLI source files are reintroduced under the Linux target.
- [x] Verify the bundled agent skill and command reference already describe the restored arguments/output; update the
  source-of-truth files under `agterm/Resources/agent-skill/` if any Linux-facing example is stale.

### Task 9: Port upstream v0.14.0 Pi status-hook support to Linux

Inherit the host-free Pi constants and ownership policy unchanged, then adapt the installer through
`LinuxIntegrations`.
Pi must participate in the existing preflight, conflict, race, rollback, CLI, and preferences workflows rather than
receive a one-off GTK installer.

- [x] Verify the merged `AgentHooksInstall.piExtensionRelativePath`, destination helpers, ownership marker, overwrite
  policy, and their upstream tests match `v0.14.0` exactly.
- [x] Verify `agterm/Resources/agent-status/pi/agterm-status.ts` is staged in every Linux package and retains the
  upstream lifecycle semantics: `agent_start` reports active/blinking and `agent_settled` reports completed/auto-reset.
- [x] Add Pi to `IntegrationKind`, status snapshots, the Integrations preferences page, CLI text/JSON output, and the
  combined hooks plan.
- [x] Keep Pi installation driveable through the existing offline `agtermctl integration install hooks` path.
  Record the control-socket exemption explicitly: integration installation edits host configuration without a running
  agterm instance, so a `Command` case would be both less reliable and the wrong ownership boundary.
- [x] Install to `~/.pi/agent/extensions/agterm-status.ts` only when `~/.pi/agent` already exists.
  Create the extensions directory as needed, preserve an unmarked same-named user file as a conflict, refresh only an
  agterm-marked file, and avoid a backup for a managed stateless extension just as upstream does.
- [x] Extend `IntegrationFilesystem` with the smallest atomic, fingerprint-validated file-copy/write operation needed
  for the Pi extension.
  Validate source and destination fingerprints between preflight and apply and preserve symlink safety.
- [x] Report absent Pi, installed, current, update available, user-owned conflict, unreadable, and write-failed states
  without aborting independently safe Claude, Codex, or shell work.
- [x] Add Linux integration tests for every state above, idempotent reinstall, source mutation after preflight,
  destination mutation after preflight, rollback, JSON status, and isolated-HOME CLI installation.
- [x] Extend the AT-SPI preferences scenario to create an isolated `.pi/agent`, inspect the Pi row and preflight, apply
  the hooks plan, and verify the extension plus ownership marker without writing outside the test HOME.

### Task 10: Port all v0.14.0 dashboard and modal-title UX to GTK

Bring the Linux dashboard to feature parity with upstream's single-click entry, title-bar opener, solid modal
presentation, and stripped dashboard/terminal-zoom titles.
Use native libadwaita header and toolbar composition rather than copying SwiftUI layout mechanics.

- [x] Change a dashboard cell's single click to highlight immediately, flash the selected frame for 180 ms, then enter
  the exact session/pane and close the dashboard.
  Keyboard Enter remains immediate because the keyboard highlight is already visible.
- [x] Give the delayed click an owner-scoped generation/source id so a second click, arrow navigation, reconcile,
  dashboard close, or window close cancels the stale selection safely.
- [x] Add the existing `agterm-grid-symbolic` Dashboard button to the content header, disabled when the window has no
  sessions, wired to the same `toggleDashboard()` path as the keymap, palette, menu, and control-opened dashboard.
- [x] Match upstream grouping and order with native separators: recent/attention, session view controls
  (scratch/split), then window-overlay controls (dashboard/quick).
- [x] Replace the transparent dashboard margin with the resolved terminal-theme background and restore the subtle top
  hairline so the grid reads as one modal surface without a black scrim or translucency bleed-through.
- [x] Build stripped native dashboard chrome that shows `Dashboard` plus a custom window name when applicable and an
  Exit Dashboard button, while removing unrelated controls and preserving GTK window drag behavior.
- [x] Add the normal active-session/window title to stripped terminal-zoom chrome beside Exit Terminal Zoom.
  Preserve hidden-toolbar behavior and avoid reparenting the live terminal more than the existing zoom contract does.
- [x] Keep dashboard and terminal zoom mutually exclusive, view-only, focus-safe, and pane-exact.
  Confirm the existing `dashboard` control command and read-back cover the behavior; document that the button and
  titles need no new control command.
- [x] Add Linux unit tests for click cancellation/last-click-wins and title derivation.
  Extend AT-SPI coverage for the header button, one-click entry, exact split-pane selection, keyboard navigation,
  dashboard title, zoom title, close buttons, and dashboard/zoom mutual exclusion.

### Task 11: Enable Swift 6 checking and make real GTK smoke tests release gates

Remove the Linux package's Swift 5 language-mode debt and close the CI gap where the existing AT-SPI suite runs only
by hand.
The same compiler and realistic UI checks must protect branch builds and the release workflow.

- [x] Update `agterm-linux/Package.swift` to Swift tools 6.0 and `swiftLanguageModes: [.v6]` while retaining the CI
  toolchain pin at Swift 6.3.2.
- [x] Fix strict-concurrency diagnostics with real ownership and actor boundaries.
  Do not add blanket `@unchecked Sendable`, global `nonisolated(unsafe)`, or synchronous UI access merely to silence
  the compiler; document the narrow C-callback exceptions that remain necessary.
- [x] Verify both Debug and Release/WMO builds because callback captures and non-Sendable values can fail only under
  optimization.
- [x] Add an isolated `scripts/test-linux-ui.sh` wrapper for the AT-SPI suite using a private HOME, state directory,
  control socket, D-Bus session, accessibility bridge, X11/Xvfb or supported compositor, and software GL.
- [x] Add the required GTK accessibility, Python GI, D-Bus, Xvfb, Mesa, and test-shell packages to Linux CI and the
  release workflow from their Ubuntu 24.04 package repositories.
- [x] Run the full AT-SPI scenario set in branch CI after the Linux app/CLI build and again in the tag workflow before
  packaging.
  Preserve diagnostics and the accessibility-tree dump as artifacts on failure.
- [x] Add CI steps for `scripts/check-linux-core-boundary.sh`, deterministic Ghostty resource verification,
  `git diff --check`, shared CLI drift protection, and Release/WMO build.
- [x] Give the branch-CI job that runs tag-boundary and `v0.14.0...HEAD` checks a full tagged checkout
  (`fetch-depth: 0` and `fetch-tags: true`, or an explicit equivalent fetch), and assert `v0.14.0` resolves before
  running those checks.
- [x] Extend the branch path filter to include packaged source-of-truth resources under
  `agterm/Resources/agent-status/**` and `agterm/Resources/agent-skill/**`, so a Pi-hook or bundled-skill-only change
  cannot skip Linux tests, staging validation, and the build.
- [x] Keep the Ghostty cache key tied to its exact revision, patch, setup script, and resource manifest, and verify a
  restored cache before trusting it.
- [x] Run all new regression scenarios from Tasks 5, 6, 9, and 10 under CI without touching a real user HOME,
  notification center, sessions, or window state.

### Task 12: Complete the parity audit and synchronize documentation

Close the upstream inventory only after every v0.14 change and every review finding has a tested disposition.
Keep public docs aligned without turning Linux into a separate product or adding downstream prose to the shared
changelog.

- [x] Reconcile the Task 1 inventory against the final `v0.13.0..v0.14.0` name-status diff, commit log, upstream release
  body, and changelog.
  Mark each item inherited, adapted, documented as macOS-only, or deliberately excluded because it is post-tag.
- [x] Confirm Pi hooks and dashboard UX are the only v0.14 product adaptations needed beyond directly inherited
  shared/docs changes, and investigate any mismatch rather than assuming the initial inventory was complete.
- [x] Map every deep-review finding to its implementing task, tests, and final validation evidence:
  packaged resources, URL length, C-string lifetimes, per-window callbacks, CLI drift, notification focus/order,
  closed-window reveal, custom-command failures, Swift 6 mode, AT-SPI CI, and patch whitespace.
- [x] Audit the final downstream diff from `v0.14.0` and verify Linux-only changes stay within approved directories.
  List each intentional portable package seam separately and confirm it has no host dependency.
- [x] Update Linux build/install/package documentation for deterministic Ghostty resources and the safe `TERM`
  fallback.
- [x] Update README and troubleshooting content for Linux Pi installation/reload, dashboard single-click/button/titles,
  notification reveal, and any corrected CLI examples.
- [x] Verify the bundled agent skill, command reference, examples, and troubleshooting mirror the final CLI and Pi
  behavior.
  Update `agterm/Resources/agent-skill/` only from its source-of-truth files, never an installed copy.
- [x] Verify inherited upstream `site/` and shared docs already contain the v0.14 Pi/dashboard updates.
  Make Linux-specific site edits only if the fork actually publishes the affected page.
- [x] Prepare `packaging/linux/release-notes/v0.14.0.md` with verified Linux adaptations, safety fixes, artifact formats,
  and compatibility.
  Do not embed a source-commit placeholder in this tracked file: `scripts/write-linux-release-notes.sh` appends the
  supplied commit dynamically, and a commit cannot contain its own final object id.
  Keep upstream product notes separate and do not edit `CHANGELOG.md` downstream.
- [x] Pin the exact approved upstream product section in
  `packaging/linux/release-notes/v0.14.0-upstream.md` before the candidate freeze.
  Compare it with `gh release view v0.14.0 --repo umputun/agterm`, exclude only upstream's macOS signing/install
  boilerplate, and verify `scripts/write-linux-release-notes.sh` prefers this committed snapshot over the live API so
  tag-time publication cannot change after approval.
- [x] Add and test `scripts/verify-published-linux-release.sh` before the candidate freeze.
  It must accept the tag, create a fresh directory with `mktemp -d`, clean it with a trap, reject missing or unexpected
  artifacts, fail on any checksum or per-artifact attestation error, and never reuse a fixed `/tmp` payload directory.
  Cover its artifact-set and failure behavior with shell fixtures or an injectable download directory so Task 13 can
  validate the verifier without an already-published release.
- [x] Generate a complete preview in `/tmp` with `scripts/write-linux-release-notes.sh` and the current candidate id,
  compare its upstream section with the committed `v0.14.0-upstream.md` snapshot and the current upstream release,
  and leave the generated output untracked so release-note inspection cannot change the candidate commit.
- [x] Make `.github/workflows/release-linux.yml` publish exactly the reviewed `body_path` by removing
  `generate_release_notes: true` and its now-unused `previous_tag` input.
  The curated generator already supplies upstream and Linux sections; allowing GitHub to append generated notes would
  publish text that was absent from the approval draft.

### Task 13: Validate the candidate, obtain the branch gate, and monitor CI

Run the complete candidate through fast tests, strict boundary checks, real GTK flows, clean packaging, and a final
diff audit before updating either protected fork branch.
The exact commits and evidence shown at the approval gate must be the commits that are pushed.

- [x] Install or verify Swift 6.3.2 and SwiftLint 0.65.0 through `mise`, plus the documented native build, UI-test, and
  packaging dependencies.
  Do not silently install privileged system packages.
- [x] If the host lacks package-only tools or compatible system libraries, reproduce the release workflow in a
  throwaway Ubuntu 24.04 container and run the same packaging and verification commands there.
  Do not waive local candidate-package verification merely because nFPM, RPM tools, or linuxdeploy are absent on the
  workstation.
- [x] Run every non-publication command in `Validation Commands`, including core tests, shared CLI tests, Linux tests,
  lint, deterministic resource setup, debug builds, Release/WMO build, the complete AT-SPI suite, diff hygiene, and
  all package formats.
- [x] Run the updated package verifier against tar, DEB, RPM, and AppImage and record theme counts, known theme files,
  `infocmp` results, runtime-library closure, CLI help, metadata, and checksums for each extracted payload.
- [x] Launch one isolated development instance with a temporary HOME, `AGTERM_STATE_DIR`, control socket, and app id.
  Validate dashboard appearance/one-click behavior, terminal-zoom title, multi-window callback ownership,
  notification suppression/reopen, custom-command errors, Pi preflight, terminal input, resize/font behavior, and
  control-socket workflows without touching live user state.
- [x] Run `scripts/check-linux-core-boundary.sh v0.14.0`, inspect every reported portable seam, verify the protected
  test is unchanged, and confirm no prohibited imports or Linux fork code entered shared core.
- [x] Inspect the full downstream diff and final worktree.
  Report deliberately excluded post-tag upstream commits, the Linux Dock-bounce exemption, all commits, all tests,
  package evidence, unrelated files left untouched, and any remaining limitation.
- [x] Present the required branch-push gate with the exact old and new object ids for `origin/master` and
  `origin/linux-port`, the proposed `git push` command, and the full validation state.
  Stop until the user explicitly approves.
- [x] After approval, push local `master` exactly to upstream `v0.14.0` and push the validated candidate to
  `origin/linux-port` without force.
- [x] Monitor the fork's Linux branch workflow to a terminal result with explicit `--repo melonamin/agterm-linux`.
  Inspect job logs and artifacts, not only the summary check color.
- [x] If CI requires a code change, make a narrow fix, rerun affected and full candidate gates, report the new commit,
  and obtain a fresh push approval before updating the branch again.
- [x] Record the final green workflow URL and verify its tested commit exactly matches `origin/linux-port`.

### Task 14: Publish and independently verify linux-v0.14.0

Cut the immutable Linux tag only from the pushed, green candidate and remain with the release workflow until the
public artifacts are proven usable.
Do not invoke inherited macOS release tooling and never move a tag after a GitHub release has been published from it.

- [x] Confirm `origin/linux-port` is the validated commit, branch CI is green for that exact object, local state is
  clean, and neither local/origin tag nor fork release `linux-v0.14.0` already exists.
- [x] Regenerate the full release body into `/tmp` with the exact candidate commit passed to
  `scripts/write-linux-release-notes.sh`, verify every feature/fix claim against completed tests and package contents,
  and confirm this inspection leaves the tracked candidate clean.
- [x] Present the required release gate with the exact annotated tag command, tag target commit, complete release
  text, expected artifacts, branch workflow URL, local package verification, and confirmation that the tag is new.
  Stop until the user explicitly approves both publishing the text and pushing the tag.
- [x] After approval, create annotated tag `linux-v0.14.0` at the approved candidate and push only that tag to
  `origin`.
  Do not create a release manually; `.github/workflows/release-linux.yml` is the publisher.
- [x] Monitor the tag workflow to a terminal state.
  If infrastructure alone fails, rerun the existing tag; if source or workflow changes are required, follow the
  immutable-tag recovery policy and never move a published tag.
- [x] Verify the GitHub release resolves to `linux-v0.14.0` and the intended commit and contains tar, DEB, RPM,
  GTK-bundled AppImage, consolidated SHA-256 file, and GitHub attestations.
- [x] Fetch the published release body and diff it byte-for-byte against the approved generated draft, allowing only a
  final-newline normalization if GitHub applies one.
  Treat any appended or changed public text as a release-verification failure.
- [x] Download all public artifacts, run the published checksum file, and verify every attestation against
  `melonamin/agterm-linux`.
- [x] Invoke the already committed and candidate-tested `scripts/verify-published-linux-release.sh` for the public tag;
  do not introduce or edit release-verification tooling after the candidate freeze.
- [x] Run `scripts/verify-linux-packages.sh` on the downloaded artifacts and independently inspect each format for the
  pinned themes, shell integration, `xterm-ghostty` terminfo, Pi resource, bundled skill, version metadata, libghostty,
  Swift runtime, GTK/libadwaita where applicable, and working `agtermctl --help`.
- [x] Install the DEB and RPM and run `agtermctl --help` in clean compatible containers when a container runtime is
  available; extract the AppImage and verify its complete runtime closure and launchers without relying on host
  Ghostty resources.
- [x] Report the public release URL, workflow URL, tag object, source commit, artifact names/sizes, resource counts,
  checksums, attestations, clean-container results, and the intentional Linux-only exemptions.
- [x] Only after successful publication and verification, move this plan to `docs/plans/completed/` in a separate
  post-release documentation commit.
  Do not amend the release commit or move the release tag; present the normal branch-push gate before publishing that
  follow-up commit.
